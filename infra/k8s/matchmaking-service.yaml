apiVersion: apps/v1
kind: Deployment
metadata:
  name: matchmaking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: matchmaking-service

  template:
    metadata:
      labels:
        app: matchmaking-service

    spec:
      containers:
        - name: matchmaking-service
          image: ghcr.io/M-Ali-Fouladi/matchmaking-service:e1ae8e99d3b3a0cb676af2ea1ed8cf18e21a1e99
          imagePullPolicy: Always

          ports:
            - containerPort: 8000

          env:
          
            - name: REDIS_URL
              value: "redis://redis:6379"

---
apiVersion: v1
kind: Service
metadata:
  name: matchmaking-service

spec:
  selector:
    app: matchmaking-service

  ports:
    - port: 80
      targetPort: 8000

  type: ClusterIP